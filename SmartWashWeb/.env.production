# 生产环境变量（npm run build / vite preview 加载）

# API 地址：编译时由 SMART_WASH_BASE_URL 环境变量注入（与 Android 端 -PbaseUrl 对齐）；
# 未设置时 vite.config.js 使用默认值。此处不再写死，避免明文 IP 入库。
# 生产构建示例：SMART_WASH_BASE_URL=https://your-domain.com/ npm run build

# 高德地图 Web端(JS API) key，供 RegionCascader / AmapPicker 组件使用
# 注意：key 一旦随 git 入库即视为泄露，上线前必须到高德控制台轮换
# TODO: 仓库与 git 历史中均未找到历史 key 值（index.html 仅内联过 securityJsCode），
#       需到高德控制台创建/轮换 Web端(JS API) key 后填入，否则地图选点/行政区划功能不可用
VITE_AMAP_KEY=

# 高德地图安全密钥（securityJsCode），由 index.html 构建时注入 window._AMapSecurityConfig
# 注意：旧值已随 git 入库视为泄露，上线前必须到高德控制台轮换
VITE_AMAP_SECURITY_CODE=cc4d5ecf17753529fc46880a4c102cae
